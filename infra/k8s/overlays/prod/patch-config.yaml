apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  ENVIRONMENT: 'production'
  LOG_LEVEL: 'info'
  CORS_ORIGINS: 'https://app.example.com'
  NEXT_PUBLIC_API_URL: 'https://app.example.com'
